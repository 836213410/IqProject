<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.rt.mapper.BigdataMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.rt.vo.Bigdata">
        <result column="warehouseid" property="warehouseid" />
        <result column="commodityid" property="commodityid" />
        <result column="comodityname" property="comodityname" />
        <result column="color" property="color" />
        <result column="size" property="size" />
        <result column="number" property="number" />
        <result column="chima" property="chima" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        warehouseid, commodityid, comodityname, color, size, number, chima
    </sql>

</mapper>
